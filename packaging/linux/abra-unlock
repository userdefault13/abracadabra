#!/bin/sh
# Prepend common user bin dirs for non-interactive SSH, then unlock the vault.
set -eu
if [ -d "${HOME}/.local/share/mise/shims" ]; then
  PATH="${HOME}/.local/share/mise/shims:${PATH}"
fi
if [ -d "${HOME}/.local/bin" ]; then
  PATH="${HOME}/.local/bin:${PATH}"
fi
export PATH
exec abra unlock "$@"
